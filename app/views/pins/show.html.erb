<div class="row">
	<div class="span6 offset3">
		<div class="well">
		<p>
		  
		  <%= @pin.description %>
		   <%= @pin.user.name %>
		   <%= image_tag @pins.photo.url %>
		</p>
		<% if current_user == @pin.user %>
		<%= link_to 'Edit', edit_pin_path(@pin) %> |
		<% end %>
		<%= link_to 'Back', pins_path %>
		<div>
	</div>
</div>


